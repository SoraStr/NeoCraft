RCON是一个基于TCP的应用层协议，理解其核心的数据包结构是成功调用的基础。

字段名称	字段类型	说明
Length	32位整数 (小端序)	表示整个数据包中 除Length字段本身外 剩余部分的字节长度。
Request ID	32位整数 (小端序)	客户端生成的唯一标识符，用于匹配请求和响应。
Type	32位整数 (小端序)	数据包类型：3=登录请求，2=执行命令，0=命令响应。
Payload	字节数组 (UTF-8)	实际数据（登录时为密码，命令时为命令文本）。
Padding	字节	填充符。为 0x00 或 0x0000，用于数据包对齐。
